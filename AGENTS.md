# Agent instructions

## Releasing a new version

When the user asks to **release**, **bump version**, or **tag a release**, read and follow:

**[.cursor/skills/release/SKILL.md](.cursor/skills/release/SKILL.md)**

Do not push version tags or edit `CloudronVersions.json` without following that checklist. Docker builds are tag-triggered only (`v*.*.*`).
